// do not use barrel imports unless your bundler supports tree shaking. using them will increase
// build time also, so if builds are taking too long then remove the barrel imports below
export * as weather from './weather';
export * as home from './home';
